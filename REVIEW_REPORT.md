# KB Indonesia AI FDS PoC — 종합 리뷰 보고서

> **리뷰 일자**: 2026-04-03
> **리뷰 팀**: Code Reviewer, Security Reviewer, Architecture Reviewer

---

## 1. 종합 평가

| 영역 | 등급 | 요약 |
|------|------|------|
| **아키텍처** | ✅ GOOD | 당근페이 3계층 패턴 충실 구현 |
| **코드 품질** | ⚠️ NEEDS WORK | 구조 우수, 성능/검증 부족 |
| **보안** | 🔴 CRITICAL | 인증 부재, API 키 노출, 프롬프트 인젝션 |

**PoC로서의 완성도는 높음**. 당근페이 벤치마크의 핵심 패턴(레고 블록 룰엔진, XML 구조화 프롬프트, 수동 RAG 파이프라인)을 충실히 구현. 다만 **프로덕션 전환 전 반드시 해결해야 할 이슈 15건** 식별.

---

## 2. CRITICAL (즉시 조치 — 4건)

### C1. OpenAI API 키 노출
- **위치**: `.env` 파일에 실제 API 키 평문 저장, `.gitignore` 부재
- **영향**: Git 커밋 시 키 유출 → 무제한 비용 발생 가능
- **조치**: 키 즉시 폐기/재발급, `.gitignore` 생성

### C2. 전체 API 인증/인가 부재
- **위치**: `api/routes.py` 전체, `main.py`
- **영향**: 네트워크 접근 가능한 누구나 규칙 비활성화, 임계값 변경, 거래 이력 조회 가능
- **조치**: JWT 또는 API Key 기반 인증 + RBAC 구현

### C3. CORS 와일드카드 (`allow_origins=["*"]`)
- **위치**: `main.py:43-48`
- **영향**: 인증 부재와 결합 시 임의 웹사이트에서 FDS 무력화 가능
- **조치**: 특정 도메인만 허용

### C4. LLM 프롬프트 인젝션
- **위치**: `llm/prompts.py:86` — `txn.memo` 필드가 검증 없이 프롬프트에 삽입
- **영향**: 공격자가 memo에 지시문을 넣어 사기 탐지 우회 가능
- **조치**: 입력 필드 새니타이징 (XML/JSON 제어 문자 제거, 길이 제한)

---

## 3. HIGH (1~2주 내 조치 — 7건)

| # | 이슈 | 위치 | 조치 |
|---|------|------|------|
| H1 | **거래당 DB 쿼리 ~12회** | `conditions.py`, `engine.py` | 인메모리 캐싱 (TTL 30초) |
| H2 | **Rate Limiting 없음** | `api/routes.py` 전체 | `slowapi` 등으로 요청 제한 |
| H3 | **PII 평문 저장** | `db/database.py` | 계좌번호 마스킹, DB 암호화 |
| H4 | **HTTPS 미적용** | `main.py:61` | TLS 인증서 적용 / 리버스 프록시 |
| H5 | **LLM이 룰엔진 무시 가능** | `api/routes.py:191-206` | 블랙리스트 등 강제 차단 규칙은 LLM 결과와 무관하게 적용 |
| H6 | **거래 금액 양수 제약 없음** | `models/schemas.py:42` | `Field(gt=0)` 추가 |
| H7 | **고객 데이터 OpenAI 전송** | `llm/evaluator.py` | PII 익명화 후 전송, 프로덕션은 온프레미스 LLM 검토 |

---

## 4. MEDIUM (3~4주 내 개선 — 8건)

| # | 이슈 | 위치 | 조치 |
|---|------|------|------|
| M1 | 보안 감사 로깅 미흡 (API 접근 로그 없음) | `api/routes.py` | 요청 미들웨어 로깅 추가 |
| M2 | LLM 에러 시 내부 정보 노출 | `llm/evaluator.py:113` | 클라이언트에 generic 에러 반환 |
| M3 | SQLite 동시 쓰기 제한 | `db/database.py` | WAL 모드 설정, 프로덕션은 PostgreSQL |
| M4 | 배치 엔드포인트 크기 제한 없음 | `api/routes.py:67` | `max_length=100` 제한 |
| M5 | LLM 호출 타임아웃 없음 | `llm/evaluator.py:74` | 3초 타임아웃 설정 |
| M6 | Streamlit 대시보드 인증 없음 | `dashboard/app.py` | streamlit-authenticator 또는 SSO |
| M7 | 테스트 코드 부재 | 전체 | 단위/통합 테스트 추가 |
| M8 | PPATK CTR/STR 보고서 자동 생성 미구현 | 미구현 | LTKM/LTKT 보고 모듈 |

---

## 5. 아키텍처 강점

리뷰어들이 공통으로 높게 평가한 사항:

| 항목 | 평가 |
|------|------|
| **당근페이 3계층 패턴 구현** | Condition → Rule → Policy 충실 구현, 로직(코드)과 설정(DB) 분리 우수 |
| **XML 구조화 프롬프트** | `<role>`, `<evaluation_flow>`, `<evaluation_criteria>` 등 정확한 패턴 적용 |
| **수동 RAG 파이프라인** | Retrieve + Prompt + API 수동 조합 (당근페이 Step 3 그대로) |
| **1건=1파일 임베딩** | JSONL 청킹 문제 방지, 12건 인도네시아 특화 사례 |
| **동적 설정 관리** | 규칙 점수/활성화/임계값을 DB로 관리, 관리자 UI + 감사 로그 |
| **비용 최적화** | 룰엔진 1차 필터링 후 고위험만 LLM 호출 (테스트 시 10건 중 1건만 LLM) |
| **Fallback 처리** | LLM 실패 시 룰엔진 결과로 대체 평가 |

---

## 6. 프로덕션 전환 로드맵

| 우선순위 | 작업 | 난이도 | 기간 |
|---------|------|--------|------|
| **P0** | API 키 폐기, `.gitignore`, 프롬프트 인젝션 방어 | 낮음 | 1일 |
| **P1** | API 인증/인가 + CORS 수정 | 중간 | 1주 |
| **P2** | 룰엔진 설정 캐싱 (성능 12x 개선) | 낮음 | 2~3일 |
| **P3** | 입력 검증 강화 (금액, 파라미터, 배치 크기) | 낮음 | 2~3일 |
| **P4** | LLM 타임아웃 + 서킷브레이커 | 낮음 | 1~2일 |
| **P5** | TLS 적용 + PII 마스킹 | 중간 | 1주 |
| **P6** | SQLite → PostgreSQL + 커넥션 풀링 | 중간 | 1~2주 |
| **P7** | 비동기 이벤트 처리 경로 (Kafka/SQS) | 높음 | 2~3주 |
| **P8** | PPATK CTR/STR 자동 보고 모듈 | 중간 | 1~2주 |
| **P9** | 테스트 스위트 + CI/CD | 중간 | 1~2주 |
| **P10** | OpenAI → AWS Bedrock Claude 전환 | 중간 | 1~2주 |
